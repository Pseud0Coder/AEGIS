import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aegis.game',
  appName: 'Aegis',
  webDir: 'src',
  bundledWebRuntime: false
};

export default config;
